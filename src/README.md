# PortPulse Source Code Directory

This directory contains the full application source code:

- `backend/`: FastAPI backend containing the modular service routers (`ingestion`, `master-data`, `forecast`, `risk`, `recommendation`, `optimiser`, `plan`, `chat`, `audit`).
- `frontend/`: React + Vite application providing the supervisor cockpit UI.
- `.env.example`: Environment variables template.
